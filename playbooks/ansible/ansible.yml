#--- Installing ansible ----

- name: set up ansible
  hosts: ansible
  tasks: 
    - name: ensure ansible is at the latest version
      become: true
      dnf:
        name: ansible-core
        state: latest